import 'package:flutter/material.dart';

import '../../consts/colors.dart';

class TopGameScreen extends StatelessWidget {
  const TopGameScreen({super.key});

   @override
  Widget build(BuildContext context) {
    return Container(
      color: whiteColor,
    );
  }
}
